apiRevision: edurata.io/v1
name: zip-folder
description: |
  Helper function to zip a folder into zip
runtime: nodejs20
interface:
  inputs:
    properties:
      file:
        type: file
        description: The file
    required: [file]
  outputs:
    properties:
      zipped:
        type: file
        description: the zipped file
